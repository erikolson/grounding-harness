"""Reference implementations of the three seams.

Nothing here is part of the harness. The core `grounding` package defines the
contracts (the Protocols in grounding.seams); this directory shows how to
satisfy them. When you compose the harness into another app, you depend on
`grounding` and write an adapter like these, or copy one.
"""
